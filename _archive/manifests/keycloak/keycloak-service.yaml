apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: le-security
  labels:
    app: keycloak
    project: linkedeye
spec:
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30084
      name: http
  selector:
    app: keycloak
  type: NodePort
